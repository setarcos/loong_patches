diff --git a/PKGBUILD b/PKGBUILD
index d2a8c3f..f9bb1ef 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -101,7 +101,7 @@ package() {
 
   # add lib symlinks
   ln -s usr/lib lib
-  [[ $CARCH = 'x86_64' ]] && {
+  [[ $CARCH = 'x86_64' || $CARCH = 'loong64' ]] && {
     ln -s usr/lib lib64
     ln -s lib usr/lib64
   }
